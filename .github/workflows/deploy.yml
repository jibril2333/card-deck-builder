name: Deploy to Docker

# SECURITY: this job runs on a SELF-HOSTED runner (the owner's own Mac, the
# same machine that serves the app). It must NEVER trigger on pull_request /
# pull_request_target — those events run untrusted contributor code and would
# hand arbitrary code execution to anyone opening a PR against this public
# repo. `push` to `main` is safe because only the repo owner has push access
# (verified: sole collaborator, admin). Do not broaden the trigger without
# re-checking collaborator/branch-protection state.
on:
  push:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: deploy-prod
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: [self-hosted, cdb]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Use a credential-helper-free Docker config
        # The host's ~/.docker/config.json sets `credsStore: "desktop"`, which
        # makes BuildKit shell out to `docker-credential-desktop` even for
        # ANONYMOUS pulls of public images. That binary lives inside Docker.app
        # and is NOT on the self-hosted runner's minimal PATH, so the build
        # dies at "load metadata". We only ever pull public images here, so
        # point DOCKER_CONFIG at a throwaway config with no credsStore — the
        # pull goes out anonymously and the whole class of failure disappears.
        run: |
          mkdir -p "$RUNNER_TEMP/dockercfg"
          printf '{}' > "$RUNNER_TEMP/dockercfg/config.json"
          echo "DOCKER_CONFIG=$RUNNER_TEMP/dockercfg" >> "$GITHUB_ENV"

      - name: Build image
        run: docker compose build

      - name: Deploy (recreate container with the new image)
        # The runner checks the repo out to its own _work dir, which has no
        # data.nosync (gitignored). Mount the real DBs from the project dir by
        # absolute path so the container sees the owner's actual data.
        run: |
          CDB_DATA_DIR="$HOME/card-deck-builder/data.nosync" \
            docker compose up -d --remove-orphans

      - name: Wait for health check
        run: |
          for i in $(seq 1 30); do
            if curl -fs http://localhost:3001/digimon > /dev/null; then
              echo "up"; exit 0
            fi
            sleep 2
          done
          echo "app did not become healthy in time" >&2
          docker compose logs --tail=100
          exit 1

      - name: Prune dangling images
        if: always()
        run: docker image prune -f
